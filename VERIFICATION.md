# 本地验证报告

验证日期：2026-09-22。环境：Windows、Node.js 24.19.0、pnpm 10.30.3、Microsoft Edge 无头浏览器。

最终源码通过独立类型检查、38 项核心测试、10 项浏览器验收、根目录构建和 /check/ 子目录构建。生产依赖审计为 **0 个已知漏洞**。以上为初始本地验收记录。随后已创建 print-happy/personal-learning-blog 仓库、上传源码并启用 GitHub Actions Pages 部署；线上构建与部署状态见仓库 Actions。在线写作台尚未使用真实个人令牌实测。

## 与原方案对应的验收结果

| 原方案验收项 | 实际证据 |
| --- | --- |
| 首页、文章、专题、关于、目录、代码高亮、搜索 | 静态页面实际构建；浏览器打开首页、筛选专题、搜索正文和导航文章。文章页有目录与高亮 |
| 日/夜主题与手机适配 | 1440×1000 桌面和 390×844 手机视口截图；手动主题刷新后保留；手机页面与编辑器检查无整页横向溢出 |
| 键盘与减少动态效果 | Ctrl+K 打开搜索，Esc 关闭；表单与按钮使用原生语义和焦点样式；减少动态效果媒体条件下检查页面 |
| 本地 Markdown、文件夹 / ZIP、图片与 GIF | ZIP 中嵌套相对路径、引用式链接、含空格文件名、缺失图片提示；GIF 导入导出逐字节一致 |
| 编辑、粘贴 / 拖入、图片管理 | 浏览器派发真实 ClipboardEvent / DragEvent 并产生资源和预览；删除图片后提示缺失，草稿仍可保存；链接插入可用 |
| 草稿保存、恢复与导出 | 页面刷新恢复正文；ZIP 下载后再次通过浏览器导入，标题与 GIF 预览正确；IndexedDB 与 localStorage 均不含测试 token |
| PDF 文字 / 扫描 / 混合 / 图片 | 对 tests/fixtures/text.pdf、scan.pdf、mixed.pdf、columns.pdf 实際导入。文字页得到段落、标题、列表与独立 PNG；扫描页不产生 OCR 文字；混合件逐页处理；多栏件附页面图像 |
| 原 PDF 原样保留 | 原样模式只生成原文件资源；转换时保留原文件；文章包往返保留未被正文引用的 PDF 字节；资源面板不提供原 PDF 删除与替换 |
| 发布确认与保存 / 构建 / 部署区分 | 浏览器模拟 GitHub API，预览确认前按钮不可用；点击后一次提交走到部署成功。核心测试分别验证上传失败、版本冲突、非强制引用更新、网络结果不确定、构建失败及部署待确认 |
| 最终文章的安全边界 | 恶意 frontmatter、script、事件属性、Vue 插值、组件与 javascript 链接经过真正构建并在浏览器完成 hydration。未执行脚本、未创建事件属性和危险链接；插值保持文字 |
| 文章 PDF 打印 | 生成 2 页普通文章 PDF 与 2 页长代码 / 宽表 PDF；提取文字检查中文、代码结束标记和文末标记；实际逐页栅格化检查，无导航和按钮，长代码及表格可读 |
| 子目录与可迁移静态托管 | /check/ 下直接打开并刷新文章，搜索、图片、原 PDF、编辑器、PDF worker / 字体资源均正常，无浏览器脚本错误或失败响应；不使用 SPA fallback |
| 读者不加载重模块 | 浏览器记录首页请求，确认没有 Editor、PDF 解析器和 PDF worker 请求；链接预取已关闭 |

## 最终检查

- 类型：独立运行 vue-tsc --noEmit，退出码 0。
- 核心：3 个测试文件，共 38 项通过。
- 浏览器：2 个测试文件，共 10 项通过，最后一次完整运行约 26 秒。
- 静态：scripts/check-static.mjs 通过，含真实构建和子目录浏览器检查。
- 依赖：pnpm install --frozen-lockfile 成功，锁文件无需变化。
- 安全版本：Vite 6.4.3、PDF.js 6.3.289、esbuild 0.25.12；生产依赖审计无公告命中。
- 本机服务：127.0.0.1:4173，首页、文章、搜索索引均返回 HTTP 200。交付时进程 ID 为 13888。
- 站点构建仅有写作模块体积提示，不是构建错误。写作模块约 1.19 MB、PDF 主模块约 488 KB（未压缩传输前的构建文件大小）；它们按需加载。首页不下载这些模块。

本机沙箱曾阻止依赖下载与构建子进程；已通过必要的受控授权恢复，没有修改系统代理或安全设置。离线启动包管理器时，其签名联网校验不可用；保持校验开启后，用正常联网的冻结锁文件安装验证成功，没有跳过签名验证。

## 截图与可复查文件

| 文件 | 内容 |
| --- | --- |
| screenshots/home-light.png | 最终日间首页 |
| screenshots/home-dark.png | 最终夜间首页 |
| screenshots/home-mobile.png | 最终手机视口首页 |
| screenshots/editor-desktop.png | 编辑器、资源与发布区 |
| screenshots/pdf-mixed.png | 混合 PDF 实际转换与逐页报告 |
| screenshots/article-print.pdf | 中文文章打印样本 |
| screenshots/long-code-print.pdf | 长代码、宽表格、中文与安全内容打印样本 |
| screenshots/print-pages.png | 上述打印文件逐页栅格化总览 |
| verification/dependency-audit.json | 最终生产依赖审计 |
| verification/static-check.json | 子目录与最终内容安全检查结果 |
| verification/browser-report.html | 最后一次完整浏览器测试报告 |

截图来自实际运行的项目；PDF 样例和站点示例文章为测试 / 演示，不代表用户材料或研究成果。

## 复现

~~~sh
pnpm install --frozen-lockfile
pnpm typecheck
pnpm test
pnpm build
pnpm preview
# 保持预览运行，在另一个终端执行：
pnpm test:e2e
pnpm test:static
~~~

本次也直接调用项目本地工具入口验证，以避开宿主应用包管理器包装器的网络限制。README 中的 pnpm 命令与本地入口调用相同的工具。

## 未验证与保留的限制

1. GitHub 仓库与 Actions 构建已实际运行并通过，Pages 发布状态见 Actions。在线写作台的个人令牌授权和受保护分支场景仍未实测，不能把模拟测试当作该流程的线上证据。
2. 浏览器验收使用 Windows Edge。手机是响应式视口模拟，未在真实 iOS Safari 或 Android 设备验证。
3. PDF 样例覆盖基本文字、扫描、混合和多栏及位图提取，不保证所有论文、复杂表格、公式、裁剪图、字体编码都能还原。实际 PDF 文字样例为英文，中文已验证段落拼接和打印，尚未系统覆盖各种中文 PDF 字体。
4. 未对真实大文件进行浏览器压力基准；体积、页数、像素与展开总量限制已经实现，超限会报错或改用原样 PDF。
5. 浏览器打印的分页会因平台和打印设置变化。当前样本的中文、图片、长代码与表格已核对，不承诺自动下载或精确页码目录。
6. 浏览器草稿只是一份本机副本；清理网站数据后须从导出文章包或已发布仓库恢复。



